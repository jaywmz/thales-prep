---
- name: Configure EC2 and deploy app
  hosts: webserver
  become: yes

  vars:
    ecr_url: "{{ lookup('env', 'AWS_ACCOUNT_ID') }}.dkr.ecr.ap-southeast-1.amazonaws.com"

  tasks:
    - name: Update apt packages
      apt:
        update_cache: yes

    - name: Install required packages
      apt:
        name:
          - apt-transport-https
          - ca-certificates
          - curl
          - software-properties-common
          - python3-pip
          - awscli
        state: present

    - name: Add Docker GPG key
      apt_key:
        url: https://download.docker.com/linux/ubuntu/gpg
        state: present

    - name: Add Docker repository
      apt_repository:
        repo: deb [arch=amd64] https://download.docker.com/linux/ubuntu jammy stable
        state: present

    - name: Install Docker
      apt:
        name: docker-ce
        state: present
        update_cache: yes

    - name: Start and enable Docker
      service:
        name: docker
        state: started
        enabled: yes

    - name: Add ubuntu user to docker group
      user:
        name: ubuntu
        groups: docker
        append: yes
      
    - name: Install Datadog agent
      include_role:
        name: datadog.dd.agent
      vars:
        datadog_api_key: "{{ lookup('env', 'DD_API_KEY') }}"
        datadog_site: "datadoghq.com"
        datadog_agent_version: "7"
        datadog_config:
          tags:
            - "env:dev"
            - "project:thales-prep"
            - "owner:wmjay"
        datadog_checks:
          docker:
            init_config:
            instances: [{}]

    - name: Login to ECR
      shell: |
        aws ecr get-login-password --region ap-southeast-1 | docker login --username AWS --password-stdin {{ ecr_url }}
      environment:
        AWS_ACCESS_KEY_ID: "{{ lookup('env', 'AWS_ACCESS_KEY_ID') }}"
        AWS_SECRET_ACCESS_KEY: "{{ lookup('env', 'AWS_SECRET_ACCESS_KEY') }}"
        AWS_DEFAULT_REGION: "ap-southeast-1"

    - name: Pull Docker image from ECR
      command: docker pull {{ ecr_url }}/thales-app:latest
      environment:
        AWS_ACCESS_KEY_ID: "{{ lookup('env', 'AWS_ACCESS_KEY_ID') }}"
        AWS_SECRET_ACCESS_KEY: "{{ lookup('env', 'AWS_SECRET_ACCESS_KEY') }}"

    - name: Remove existing container if exists
      command: docker rm -f thales-app
      ignore_errors: yes

    - name: Run Docker container
      command: docker run -d -p 5000:5000 --name thales-app {{ ecr_url }}/thales-app:latest